name: Publish Images

# Builds the add-ons for all architectures and pushes the images to GHCR.
# Once the packages are made public and config.yaml gains an `image:` key,
# Home Assistant installs pull these prebuilt images instead of building
# locally — which fixes build OOM on small boxes (#56) and stops the
# Supervisor from exporting the locally-built image into every backup
# (the second half of #103).

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "claude-terminal/**"
      - "codewhale-terminal/**"

jobs:
  publish:
    name: Publish Claude Terminal (${{ matrix.arch }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        # armv7 is not supported by current HA builder releases (32-bit ARM
        # was dropped upstream); armv7 installs keep building locally
        arch: [aarch64, amd64]
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push add-on image
        uses: home-assistant/builder@2026.02.1
        with:
          args: |
            --${{ matrix.arch }} \
            --target /data/claude-terminal \
            --image "${{ matrix.arch }}-addon-claude-terminal" \
            --docker-hub "ghcr.io/${{ github.repository_owner }}" \
            --addon

  publish-codewhale:
    name: Publish Codewhale Terminal (${{ matrix.arch }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        arch: [aarch64, amd64]
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push add-on image
        uses: home-assistant/builder@2026.02.1
        with:
          args: |
            --${{ matrix.arch }} \
            --target /data/codewhale-terminal \
            --image "${{ matrix.arch }}-addon-codewhale-terminal" \
            --docker-hub "ghcr.io/${{ github.repository_owner }}" \
            --addon
