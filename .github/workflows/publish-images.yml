name: Publish Images

# Builds the add-on for all architectures and pushes the images to GHCR.
# Home Assistant installs pull these prebuilt images instead of building
# locally — which fixes build OOM on small boxes and stops the Supervisor
# from exporting the locally-built image into every backup.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "codewhale-terminal/**"

jobs:
  publish:
    name: Publish (${{ matrix.arch }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        arch: [aarch64, amd64]
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push add-on image
        uses: home-assistant/builder@2026.02.1
        with:
          args: |
            --${{ matrix.arch }} \
            --target /data/codewhale-terminal \
            --image "${{ matrix.arch }}-addon-codewhale-terminal" \
            --docker-hub "ghcr.io/${{ github.repository_owner }}" \
            --addon
